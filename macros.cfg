

[quad_gantry_level]
gantry_corners:
	-60,-10
	360,370
points:
	25,25
	25,250
	270,250
	270,25
speed: 1000
horizontal_move_z: 10
retries: 10
retry_tolerance: 0.0025 # 0.0075
max_adjust: 10

[gcode_macro QUAD_GANTRY_LEVEL]
rename_existing: _QUAD_GANTRY_LEVEL
gcode:
    # If QGL is not applied, first run a course calibration
    {% if printer.quad_gantry_level.applied == False %}
        _QUAD_GANTRY_LEVEL RETRY_TOLERANCE=1.0
    {% endif %}
    # then perform fine QGL down to desired spec
    # this has to be a separate macro call so the results of the above call will be visible!
    _FINE_QUAD_GANTRY_LEVEL

[gcode_macro _FINE_QUAD_GANTRY_LEVEL]
gcode:
    {% if printer.quad_gantry_level.applied == True %}
        # go for full quality at reduced probing height
        _QUAD_GANTRY_LEVEL HORIZONTAL_MOVE_Z=2.0  # <- set your preferred probing height here!
    {% else %}
        # This should never happen, just perform the full calibration using the defaults
        {action_respond_info("Fine QGL called without calling course QGL first!")}
        _QUAD_GANTRY_LEVEL  # default behavior, no speedup
    {% endif %}


[gcode_macro TOOLCHANGE_DEMO]
gcode:
    {% for n in range(10) %}
      T{ printer.toolchanger.tool_numbers | random }
    {% endfor %}


[gcode_macro PRINT_START]
variable_printing: False
gcode:
  {% set TOOL         = params.TOOL       | default(0)   | int %}
  {% set TOOL_TEMP    = params.TOOL_TEMP  | default(210) | int %}
  {% set BED_TEMP     = params.BED_TEMP   | default(60)  | int %}
  {% set CHAMBER_TEMP = params.CHAMBER    | default(40)  | int %}
  {% set x_wait       = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait       = printer.toolhead.axis_maximum.y|float / 2 %}

  SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=printing VALUE=False
  M117 Initializing
  G28
  #SYNC_MOTORS
  
  # REQUIRED BY klipper-toolchanger-easy
  #STOP_CRASH_DETECTION

  # Bed heat + (optionally) nozzle preheat for TAP
  M117 Heating bed
  M190 S{BED_TEMP}
  G4 S500
  M109 T0 S150
  M191 S35
  
  # Home with toolchanger’s homing_override (handles its own init)
  M117 Homing
  SET_LED LED=chamber_lighting RED=1 GREEN=0 BLUE=0 WHITE=0
  G28
  T0

  # Quad gantry + Z home
  M117 Quad Gantry Level
  QUAD_GANTRY_LEVEL
  G28 Z
  BEACON_AUTO_CALIBRATE
  # Clear old mesh
  BED_MESH_CLEAR

  # Bed/chamber soak logic (your original)
  {% if BED_TEMP > 90 %}
    G1 X{x_wait} Y{y_wait} Z15 F9000
    SET_DISPLAY_TEXT MSG="Bed: {BED_TEMP}c"
    M106 S255
    SET_PIN PIN=nevermore VALUE=1
    M190 S{BED_TEMP}
    SET_DISPLAY_TEXT MSG="Heatsoak: {CHAMBER_TEMP}c"
    TEMPERATURE_WAIT SENSOR="temperature_sensor chamber" MINIMUM={CHAMBER_TEMP}
  {% else %}
    SET_DISPLAY_TEXT MSG="Bed: {BED_TEMP}c"
    G1 X{x_wait} Y{y_wait} Z15 F9000
    M190 S{BED_TEMP}
    SET_DISPLAY_TEXT MSG="Soak for 5min"
    G4 P300000
  {% endif %}

  # Mesh – use KAMP or plain mesh, your call
  M117 Calibrating bed
  SET_LED LED=chamber_lighting RED=0.5 GREEN=0 BLUE=0.5 WHITE=0
  BED_MESH_CALIBRATE; or BED_MESH_CALIBRATE ADAPTIVE=1 if you want KAMP adaptive
  


  # Select active tool and wait for temp (per README pattern)
  {% if TOOL >= 0 %}
    M104 T0 S0
    T{TOOL}
    {% set initialToolTemp = 'T' ~ TOOL|string ~ '_TEMP' %}
    {% if initialToolTemp in params %}
      M117 Waiting on T{TOOL} S{params[initialToolTemp]}C
      M109 S{params[initialToolTemp]}
    {% else %}
      M109 S{TOOL_TEMP}
    {% endif %}
  {% else %}
    M109 S{TOOL_TEMP}
  {% endif %}

  # REQUIRED BY klipper-toolchanger-easy
  START_TOOL_CRASH_DETECTION
  
  
  # Your purge + status stuff
  SET_DISPLAY_TEXT MSG="Printing"
  SET_LED LED=chamber_lighting RED=0 GREEN=0 BLUE=0 WHITE=1
  G0 Y{y_wait} F10000
  G4 P500
  G0 X{x_wait - 50} F10000
  G4 P500
  G0 Z1.5

  VORON_PURGE

  SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=printing VALUE=True
  M117 Printing


[gcode_macro PRINT_END]
gcode:
    {% if printer.tool_probe_endstop is defined %}
      STOP_CRASH_DETECTION
    {% endif %}

    SET_GCODE_VARIABLE MACRO=PRINT_START VARIABLE=printing VALUE=False
    M400                           ; wait for buffer to clear
    G92 E0                         ; zero the extruder
    G1 E-4.0 F3600                 ; retract filament

    G91                            ; relative positioning
    {% set max_x = printer.configfile.config["stepper_x"]["position_max"]|float - 0.5 %}
    {% set max_y = printer.configfile.config["stepper_y"]["position_max"]|float - 0.5 %}
    {% set max_z = printer.configfile.config["stepper_z"]["position_max"]|float %}

    {% if printer.toolhead.position.x < (max_x - 20) %}
        {% set x_safe = 20.0 %}
    {% else %}
        {% set x_safe = -20.0 %}
    {% endif %}

    {% if printer.toolhead.position.y < (max_y - 20) %}
        {% set y_safe = 20.0 %}
    {% else %}
        {% set y_safe = -20.0 %}
    {% endif %}

    {% if printer.toolhead.position.z < (max_z - 20) %}
        {% set z_safe = 20.0 %}
    {% else %}
        {% set z_safe = max_z - printer.toolhead.position.z - 5 %}
    {% endif %}

    G0 X{x_safe} Y{y_safe} F20000
    G0 Z{z_safe} F3600
    M400
    G92 E0
    G1 E-2.0 F1800

    TURN_OFF_HEATERS
    M107

    G90
    {% set tool = printer[printer.toolchanger.tool] %}
    {% set park_z = max_z - 5 %}
    {% if park_z < 0 %}
      {% set park_z = 0 %}
    {% endif %}
    G0 X{tool.params_park_x} Y{tool.params_safe_y} Z{park_z} F3600

    M18
    M117 Print done

[gcode_macro APPLY_AND_SAVE_NEW_CALIBRATION_OFFSETS]
description: Run after TOOL_CALIBRATE_TOOL_OFFSET.
             Stages last calibrated offsets for SAVE_CONFIG and also applies them live.
gcode:
    {% set tool         = printer.toolchanger.tool %}
    {% set last_results = printer.tools_calibrate.last_result %}
    {% set tool_obj     = printer.printer.lookup_object(tool, None) %}
    {% set cfg          = printer.printer.lookup_object('configfile', None) -%}
    {% for offset in ['gcode_x_offset', 'gcode_y_offset', 'gcode_z_offset'] if tool_obj and cfg %}
        {% set _ = tool_obj.__setattr__(offset, last_results[loop.index0]) %}
        {% set _ = cfg.set(tool, offset, last_results[loop.index0]) %}
    {% else %}
        {action_raise_error()}
    {% endfor %}


[gcode_macro UNSAFE_LOWER_BED]
description: Lower the bed 50mm without homing
gcode:
  G90
  SET_KINEMATIC_POSITION Z=50
  G0 Z0 F600
  M84

[gcode_macro UNSAFE_RAISE_BED]
description: Raise the bed 5mm without homing
gcode:
  G90
  SET_KINEMATIC_POSITION Z=0
  G0 Z5 F600
  M84



[gcode_macro TEST_SPEED]
# Home, get position, throw around toolhead, home again.
# If MCU stepper positions (first line in GET_POSITION) are greater than a full step different (your number of microsteps), then skipping occured.
# We only measure to a full step to accomodate for endstop variance.
# Example: TEST_SPEED SPEED=300 ACCEL=5000 ITERATIONS=10

description: Test for max speed and acceleration parameters for the printer. Procedure: Home -> ReadPositionFromMCU -> MovesToolhead@Vel&Accel -> Home -> ReadPositionfromMCU

gcode:
    # Speed
    {% set speed  = params.SPEED|default(printer.configfile.settings.printer.max_velocity)|int %}
    # Iterations
    {% set iterations = params.ITERATIONS|default(5)|int %}
    # Acceleration
    {% set accel  = params.ACCEL|default(printer.configfile.settings.printer.max_accel)|int %}
    # Minimum Cruise Ratio
    {% set min_cruise_ratio = params.MIN_CRUISE_RATIO|default(0.5)|float %}
    # Bounding inset for large pattern (helps prevent slamming the toolhead into the sides after small skips, and helps to account for machines with imperfectly set dimensions)
    {% set bound = params.BOUND|default(20)|int %}
    # Size for small pattern box
    {% set smallpatternsize = SMALLPATTERNSIZE|default(20)|int %}
    
    # Large pattern
        # Max positions, inset by BOUND
        {% set x_min = printer.toolhead.axis_minimum.x %}
        {% if x_min < 0 %}
            {% set x_min = 0 %}
        {% endif %}
    
        {% set y_min = printer.toolhead.axis_minimum.y %}
        {% if y_min < 0 %}
            {% set y_min = 0 %}
        {% endif %}
    
        {% set x_min = x_min + bound %}
        {% set x_max = printer.toolhead.axis_maximum.x - bound %}
        {% set y_min = y_min + bound %}
        {% set y_max = printer.toolhead.axis_maximum.y - bound %}
    
    # Small pattern at center
        # Find X/Y center point
        {% set x_center = (printer.toolhead.axis_minimum.x|float + printer.toolhead.axis_maximum.x|float ) / 2 %}
        {% set y_center = (printer.toolhead.axis_minimum.y|float + printer.toolhead.axis_maximum.y|float ) / 2 %}
        
        # Set small pattern box around center point
        {% set x_center_min = x_center - (smallpatternsize/2) %}
        {% set x_center_max = x_center + (smallpatternsize/2) %}
        {% set y_center_min = y_center - (smallpatternsize/2) %}
        {% set y_center_max = y_center + (smallpatternsize/2) %}

    # Save current gcode state (absolute/relative, etc)
    SAVE_GCODE_STATE NAME=TEST_SPEED
    
    # Output parameters to g-code terminal
    { action_respond_info("TEST_SPEED: starting %d iterations at speed %d, accel %d" % (iterations, speed, accel)) }
    
    # Home and get position for comparison later:
        M400 # Finish moves - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/66
        G28
        # QGL if not already QGLd (only if QGL section exists in config)
        {% if printer.configfile.settings.quad_gantry_level %}
            {% if printer.quad_gantry_level.applied == False %}
                QUAD_GANTRY_LEVEL
                G28 Z
            {% endif %}
        {% endif %} 
        # Move 50mm away from max position and home again (to help with hall effect endstop accuracy - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/24)
        G90
        G1 X{printer.toolhead.axis_maximum.x-50} Y{printer.toolhead.axis_maximum.y-50} F{30*60}
        M400 # Finish moves - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/66
        G28 X Y
        G0 X{printer.toolhead.axis_maximum.x-1} Y{printer.toolhead.axis_maximum.y-1} F{30*60}
        G4 P1000 
        GET_POSITION

    # Go to starting position
    G0 X{x_min} Y{y_min} Z{bound + 10} F{speed*60}

    # Set new limits
    {% if printer.configfile.settings.printer.minimum_cruise_ratio is defined %}
        SET_VELOCITY_LIMIT VELOCITY={speed} ACCEL={accel} MINIMUM_CRUISE_RATIO={min_cruise_ratio}
    {% else %}
        SET_VELOCITY_LIMIT VELOCITY={speed} ACCEL={accel} ACCEL_TO_DECEL={accel / 2}
    {% endif %}

    {% for i in range(iterations) %}
        # Large pattern diagonals
        G0 X{x_min} Y{y_min} F{speed*60}
        G0 X{x_max} Y{y_max} F{speed*60}
        G0 X{x_min} Y{y_min} F{speed*60}
        G0 X{x_max} Y{y_min} F{speed*60}
        G0 X{x_min} Y{y_max} F{speed*60}
        G0 X{x_max} Y{y_min} F{speed*60}
        
        # Large pattern box
        G0 X{x_min} Y{y_min} F{speed*60}
        G0 X{x_min} Y{y_max} F{speed*60}
        G0 X{x_max} Y{y_max} F{speed*60}
        G0 X{x_max} Y{y_min} F{speed*60}
    
        # Small pattern diagonals
        G0 X{x_center_min} Y{y_center_min} F{speed*60}
        G0 X{x_center_max} Y{y_center_max} F{speed*60}
        G0 X{x_center_min} Y{y_center_min} F{speed*60}
        G0 X{x_center_max} Y{y_center_min} F{speed*60}
        G0 X{x_center_min} Y{y_center_max} F{speed*60}
        G0 X{x_center_max} Y{y_center_min} F{speed*60}
        
        # Small pattern box
        G0 X{x_center_min} Y{y_center_min} F{speed*60}
        G0 X{x_center_min} Y{y_center_max} F{speed*60}
        G0 X{x_center_max} Y{y_center_max} F{speed*60}
        G0 X{x_center_max} Y{y_center_min} F{speed*60}
    {% endfor %}

    # Restore max speed/accel/accel_to_decel to their configured values
    {% if printer.configfile.settings.printer.minimum_cruise_ratio is defined %}
        SET_VELOCITY_LIMIT VELOCITY={printer.configfile.settings.printer.max_velocity} ACCEL={printer.configfile.settings.printer.max_accel} MINIMUM_CRUISE_RATIO={printer.configfile.settings.printer.minimum_cruise_ratio} 
    {% else %}
        SET_VELOCITY_LIMIT VELOCITY={printer.configfile.settings.printer.max_velocity} ACCEL={printer.configfile.settings.printer.max_accel} ACCEL_TO_DECEL={printer.configfile.settings.printer.max_accel_to_decel}
    {% endif %}

    # Re-home and get position again for comparison:
        M400 # Finish moves - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/66
        G28 # This is a full G28 to fix an issue with CoreXZ - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/12
        # Go to XY home positions (in case your homing override leaves it elsewhere)
        G90
        G0 X{printer.toolhead.axis_maximum.x-1} Y{printer.toolhead.axis_maximum.y-1} F{30*60}
        G4 P1000 
        GET_POSITION

    # Restore previous gcode state (absolute/relative, etc)
    RESTORE_GCODE_STATE NAME=TEST_SPEED
    